import React from 'react';
import './flights.scss';

import {IFlightsViewProps} from './FlightsContainer';

const FlightsView = (props: IFlightsViewProps) => {
   const { } = props

   return (
   <div className='flights'>

   </div>
   )
} 

FlightsView.defaultProps = {

} 

export default FlightsView;
