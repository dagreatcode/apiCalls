import React from 'react';

import {IHeaderViewProps} from './HeaderContainer';

const HeaderView = (props: IHeaderViewProps) => {
   const { } = props

   return (
   <div className='header'>

   </div>
   )
} 

HeaderView.defaultProps = {

} 

export default HeaderView;
